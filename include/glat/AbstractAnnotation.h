#ifndef ABSTRACT_ANNOTATION_H
#define ABSTRACT_ANNOTATION_H

#include <glat/RenderMethodFactory.h>

namespace glat {
class AbstractAnnotation {
	AbstractAnnotation(glat::RenderMethodFactory* factory);
};
}

#endif